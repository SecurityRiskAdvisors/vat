mutation CreateLibraryDefenseLayer(
  $input: CreateLibraryDefenseLayerInput!
  ) {
  defenseLayer {
    createLibrary(input: $input) {
      defenseLayers {
        id
        name
      }
    }
  }
}
